//textarea 라이브러리
$(document).ready(function() {
  $('#summernote').summernote({
      width:900,
      height: 500,
      minHeight: null, // 최소
      maxHeight: null, // 최대
      focus: true
  });
});